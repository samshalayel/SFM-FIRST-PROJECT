# SFM-FIRST-PROJECT

This repository contains the initial setup for a Laravel project created using `composer create-project laravel/laravel`.

## Installation

Run the following command to install Laravel:

```sh
composer create-project laravel/laravel .
```